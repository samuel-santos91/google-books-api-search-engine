import styles from "./Header.module.scss"

const Header = () => {
  return (
    <header>
        <h1>Google Books</h1>
        <p>Find out about your favourite book!</p>
    </header>
  )
}

export default Header;